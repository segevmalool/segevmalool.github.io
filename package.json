{
  "name": "segbaus.com",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "squash-build": "(find . -depth 1 | grep -v dist | grep -v '\\.git' | xargs rm -rf) && mv ./dist/* ./ && rmdir dist"
  },
  "dependencies": {
    "lit": "^3.3.0"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vite": "^6.3.5"
  }
}
